# Base de datos PROPIA de la consola (separada de ddai-web)
DATABASE_URL="postgresql://qaconsole_app:CAMBIA_ESTO@HOST:5432/qaconsole?schema=public"

# Token para autenticar la ingesta de resultados (POST /api/runs)
# Genera uno fuerte:  openssl rand -hex 32
INGEST_TOKEN="pon_un_token_largo_y_secreto_aqui"
